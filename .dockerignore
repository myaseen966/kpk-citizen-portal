# Dependencies
node_modules
npm-debug.log
npm-debug.log*

# Next.js build output
.next
out

# Environment variables — never bake secrets into the image
.env
.env.*
!.env.example

# Version control
.git
.gitignore

# Docker files (no need to copy these into the build context image layer)
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.yaml

# Kubernetes manifests (not needed inside the image)
kubernetes

# Infrastructure-as-code / CI config (not needed inside the image)
terraform
.github
scripts

# Editor / OS files
.vscode
.idea
.DS_Store
Thumbs.db

# Logs
*.log
yarn-debug.log*
yarn-error.log*

# Test / misc
coverage
.turbo
README.md
CLAUDE.md
AGENTS.md
